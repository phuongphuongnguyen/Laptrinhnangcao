#include <iostream>
#include <string>
using namespace std;
int main()
{
    string s;
    cin >>s;
    int i(0),j(s.length()-1);
    while (i<j && s[i]==s[j]) {i++;j--;}
    if(s[i]==s[j]) cout <<"yes";else cout <<"no";
    return 0;
}
